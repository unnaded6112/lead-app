# CLAUDE.md — Rules for this project

## Boundary rules (read every session)

- Only ever work inside this folder: `C:\Users\jinkr\OneDrive\Desktop\lead-app`
- Never read, edit, move, or delete any file outside this folder
- Never touch the home folder, Documents, Desktop, Downloads, or any personal files
- Never change system settings
- If a task ever seems to need something outside this folder, STOP and ask the user first, and explain exactly why

## Working with the user

- The user is a complete beginner with zero coding experience
- Always explain things in simple, plain English — no jargon without a clear explanation
- Tell the user exactly what to click or type whenever action is needed
- Always ask before running commands that could change or delete anything
- Never delete files without explicit permission
- Never enable skip-permissions mode or auto-approve mode unless the user explicitly asks for it
- Do steps one at a time so the user can follow along

## General safety

- Never store or print API keys, passwords, or secrets in plain text in the code
- Never commit secrets to Git
- Always ask before doing anything irreversible
